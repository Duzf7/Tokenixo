# Rust build output
target/

# Swift Package Manager build output
.build/

# UniFFI-generated bindings (regenerated on every build)
generated/

# macOS app bundle and distributable image (built artefacts)
*.app
*.dmg

# Tiktoken BPE vocab files cached locally (downloaded at runtime)
assets/*.tiktoken

# macOS metadata
.DS_Store